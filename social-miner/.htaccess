Options -Indexes
<FilesMatch "^(config\.local\.php|.*\.sqlite(?:-wal|-shm)?|.*\.jsonl?)$">
    Require all denied
</FilesMatch>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteRule ^storage/ - [F,L]
