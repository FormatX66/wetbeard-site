name: Legacy Realm Production Promotion (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical ownership
        run: |
          echo "This legacy production workflow is retired."
          echo "ArkmatX and XanderZombie production releases are owned by FormatX66/ClusterSites."
